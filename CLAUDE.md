# 인공지능 기초 실습실 (`22aiBasics_trmoo`)

## ① 앱의 목적과 대상 단원

고등학교 2학년 **「인공지능 기초」** 수업용 학습지
`인공지능 기초 노트 v260307.pdf`(36쪽) 를 **브라우저에서 직접 조작해 보는** 실습 앱.

학습지가 「빈칸을 채우는 종이」라면, 이 앱은 그 빈칸의 값을 **직접 만들어 보는 실험실**이다.
예를 들어 학습지가 「분산 = ( )」이라고 묻는다면, 앱에서는 숫자를 바꿔 가며
분산이 어떻게 움직이는지 본 뒤에 빈칸을 채우게 된다.

- 대상: 고등학교 2학년 「인공지능 기초」 수강생
- 성취기준: `[12인기01-02]` `[12인기02-01~03]` `[12인기03-01~05]` `[12인기04-01~04]` `[12인기05-01~02]`
- 개인정보를 수집·저장하지 않는다. localStorage 도 쓰지 않는다.

## ② 기능 목록 — 탭 6개 / 화면 23개

주소 해시(`#nn/cnn`)로 화면을 바로 링크할 수 있다.
화면 오른쪽 위 **[👁️ 정답 모두 보기]** 로 그 화면의 괄호 정답을 한꺼번에 연다.

### Ⅰ. AI 구현 프로세스 (학습지 1~5쪽) — `src/tabs/process/`
| 화면 | 무엇을 직접 하나 |
|---|---|
| `pipeline` 프로세스와 데이터 종류 | 여섯 단계 지도 클릭 / 정형·반정형·비정형 카드 나누기 / 데이터 유형 6분류 카드 나누기 |
| `stats` 기술 통계 실험실 | 자료를 고치면 14가지 통계량·점그래프·상자그림이 실시간. 분산 계산 과정 표. 왜도·첨도 모양 슬라이더 |
| `viz` 시각화와 상관계수 | 같은 자료를 5가지 그래프로 / 막대 vs 히스토그램 비교 / **점을 끌면 r 이 바뀌는 산점도** / 상관 히트맵 |

### Ⅱ. 데이터 전처리 (학습지 4~5쪽) — `src/tabs/prep/`
| 화면 | 무엇을 직접 하나 |
|---|---|
| `missing` 결측치 | 제거/대체 5가지를 눌러 표가 실제로 바뀌는 것을 봄. 손실률·표준편차 변화 표시 |
| `outlier` 이상치 | 수직선의 점을 끌면 Q1·Q3·IQR·경계 재계산. 처리 3가지 비교. **이상치 하나가 회귀선을 흔드는 실험** |
| `scaling` 정규화 | **거리 계산으로 왜 필요한지 증명** / min-max vs 표준화 나란히 / 이상치가 있을 때의 차이 |
| `encoding` 인코딩·핵심 속성 | 같은 열을 레이블/원핫으로 변환, 명목형에 레이블을 쓰면 생기는 문제 / 속성 남길지 뺄지 판정 |

### Ⅲ. 모델링과 평가 (학습지 5~8쪽 + 심화 9~12쪽) — `src/tabs/model/`
| 화면 | 무엇을 직접 하나 |
|---|---|
| `kinds` 기계학습 세 갈래 | 분류 vs 회귀 그림 / **k-평균 군집화를 한 걸음씩 돌려 봄** / 상황 카드 4분류 |
| `split` 데이터 준비와 분할 | X·y 분리(열 클릭) / 훈련:검증:평가 비율 슬라이더 / k-겹 교차검증 회차별 재생 |
| `overfit` 학습과 과적합 | **다항식 차수 1~12 를 올리며 두 오차가 갈라지는 것** / 경사하강법으로 900회 학습한 기록과 조기 중단 지점 |
| `classify` 분류 모델 평가 | 혼동행렬 계산기 / 붓꽃 3×3→2×2 / **임계값 슬라이더로 정밀도↔재현율 트레이드오프** / 정확도의 역설 |
| `regress` 회귀 모델 평가 | 점을 끌면 MAE·MSE·RMSE·R² 실시간. **MSE 를 정사각형 넓이로 그려 줌** |
| `algo` KNN·결정트리 | ★를 끌며 k 를 바꾸는 KNN / 트리를 따라 내려가는 길 표시 / 루트 노드를 정보 이득으로 고르기 |

### Ⅳ. 신경망과 딥러닝 (학습지 13~18쪽) — `src/tabs/nn/`
| 화면 | 무엇을 직접 하나 |
|---|---|
| `perceptron` 퍼셉트론 | w₁·w₂·b 슬라이더로 결정 경계를 움직임. **XOR 은 아무리 해도 안 되는 것을 몸으로 확인** / 학습 알고리즘 실제 실행 |
| `mlp` 다층 신경망 | 학습지 가중치를 그대로 계산한 XOR 표 / 두 직선이 평면을 세 조각으로 가르는 그림 / 활성화 함수 5종 / 소프트맥스 |
| `backprop` 역전파·경사하강법 | 손실 곡면 위의 공(학습률이 크면 발산) / **2-4-1 신경망이 XOR 을 스스로 배우는 3000회 기록** |
| `cnn` 합성곱 신경망 | **8×8 격자를 직접 칠하고 필터를 한 칸씩 슬라이딩** → 6×6 특징맵 + 곱셈 9개 계산식 / 풀링 |

### Ⅴ. 탐색 (학습지 19~30쪽) — `src/tabs/search/`
| 화면 | 무엇을 직접 하나 |
|---|---|
| `space` 상태 공간 | **강 건너기** — 직접 태우고 건넘, 잡아먹히면 경고, 상태 공간 그래프에 지나온 길이 칠해짐 / 하노이 2~4개 |
| `graph` BFS·DFS·UCS | OPEN·CLOSED 리스트가 한 걸음씩 바뀌는 애니메이션 / 균일 비용 탐색 표(비용 편집 가능) |
| `puzzle` 8-퍼즐 겨루기 | 직접 풀기 / 휴리스틱 2종 계산표 / **네 탐색의 「살펴본 상태 수」 비교 + 풀이 재생** |
| `astar` A* 길 찾기 | 6×6 격자에 벽을 그리고 출발·도착을 옮김. 칸마다 f=g+h. **h=0 으로 두면 균일 비용 탐색이 되는 것** |

### Ⅵ. 지식 표현·추론과 윤리 (학습지 31~35쪽) — `src/tabs/ethics/`
| 화면 | 무엇을 직접 하나 |
|---|---|
| `rules` 규칙 기반 추론 | 학습지 규칙 14개 추론 엔진. 사실을 고르면 규칙이 하나씩 발화. **학생이 규칙을 직접 추가** |
| `ethics` 인공지능 윤리 | 쟁점 5가지 사례 분류 / **편향 실험실**(전체 정확도가 소수 집단 피해를 가리는 것) / **모럴 머신 13상황 → 9축 성향 그래프** / 국내·EU 기준 |

## ③ 현재 개발 상태와 다음 할 일

**상태: 완성. 빌드·시험·브라우저 점검 모두 통과.** (2026-08-07)

- `npm test` → **98가지 통과** (학습지 정답을 지키는 회귀 시험)
- `npm run build` → `dist/index.html` 한 파일 약 275KB, 더블클릭 실행
- 브라우저 점검: 23개 화면 전부 오류 없음, 모든 단추를 눌러도 예외 0건,
  1265px·753px 두 너비에서 가로 넘침 없음

### 다음에 할 만한 일
- 학습지 36쪽 pandas 요약(`df[]`, `loc`, `drop`, `fillna`)을 실습 화면으로 (지금은 각 탭의 🐍 접이 상자에만 있음)
- 학습지 34~35쪽 모럴 머신 [4] [5] 서술 문항 (지금은 [1]~[3] 만)
- 깃허브 저장소·Pages 개설 (`.github/workflows/deploy.yml` 아직 없음)

---

## 기술 메모

### 구조
```
src/
  main.js            탭·해시 라우팅
  style.css          공통 스타일 (대단원마다 --accent 색이 바뀜)
  lib/
    ui.js            h() · card() · answer() · quizSet() · sortQuiz() · table() · slider() …
    stats.js         평균~R² 까지 모든 통계 계산 (순수 함수, 테스트가 직접 부름)
    chart.js         캔버스 그리기 도구 (외부 그래프 라이브러리 없음)
  tabs/<대단원>/<화면>.js
```

- **외부 CDN·그래프 라이브러리를 쓰지 않는다.** 교실 인터넷이 끊겨도 열리게 하려는 것.
  그래프는 모두 `chart.js` 의 캔버스 도구로 직접 그렸다.
- 빌드는 `vite-plugin-singlefile` 로 **`dist/index.html` 한 파일**에 묶는다. `base: './'`.
- 정답 가리기는 `answer()`(인라인)와 `answerBlock()`(문단 통째)의 두 가지.
  둘 다 `data-answer` 를 달고 있어 `toggleAllAnswers()` 가 한꺼번에 연다.

### 진짜로 계산하는 것들 (흉내가 아님)
| 어디 | 무엇을 |
|---|---|
| `overfit.js` | 최소제곱법(정규방정식 + 가우스 소거법)으로 1~12차 다항 회귀. 경사하강법 900회 |
| `backprop.js` | 2-4-1 신경망을 오차 역전파로 3000회 학습 (시그모이드, 배치 경사하강) |
| `kinds.js` | k-평균 군집화 (배정 ↔ 중심 이동 반복) |
| `puzzle.js` | 8-퍼즐 BFS / 깊이제한 DFS / A*(불일치 타일) / A*(맨해튼) |
| `graph.js` | BFS·DFS(OPEN/CLOSED 기록) · 균일 비용 탐색 |
| `astar.js` | 6×6 격자 A*, 한 걸음씩 기록 |
| `space.js` | 강 건너기 BFS 최적해, 안전 상태 10개 열거 |
| `rules.js` | 전방 추론(forward chaining) |
| `cnn.js` | 합성곱·풀링 |

### 시험 (`npm test`)
`test/run.mjs` 가 순수 함수를 직접 불러 **학습지 정답과 대조**한다.
화면 그리는 함수는 부르지 않으므로 브라우저 없이 node 로 돈다.
그래서 탭 모듈에서 알고리즘 함수에 `export` 를 붙여 두었다 — **지우지 말 것.**

지키고 있는 값(수치를 고칠 때 함께 갱신):

| 학습지 | 값 |
|---|---|
| 2쪽 자료 `1,1,1,1,2,3,3,5,10` | 평균 3 · 중앙값 2 · 최빈값 1 · 최솟 1 · 최댓 10 · 분산 70/9 ≈ 7.778 · 표준편차 2.789 · Q1 1 · Q3 4 · IQR 3 · 이상치 10 |
| 심화 9쪽 [활동2] 붓꽃 | 버시컬러 기준 TP 14 · FN 0 · FP 1 · TN 15 |
| 심화 9쪽 [활동3] 불량품 | 정확도 85% · 정밀도 60% · 재현율 75% · F1 2/3 |
| 14쪽 AND 퍼셉트론 (1,1,−1.5) | −1.5 / −0.5 / −0.5 / +0.5 → 0,0,0,1 |
| 15쪽 다층 XOR | y₁ 0,0,0,1 · y₂ 0,1,1,1 · y 0,1,1,0 · (1,1)의 출력 가중합 −1.9 |
| 18쪽 합성곱 | 8×8 → 6×6. 세로경계 필터로 0→1 은 +3, 1→0 은 −3, 일정한 곳 0 |
| 20쪽 강 건너기 | 최소 **7번**. 안전한 상태 10가지. 첫 수 `1010` |
| 22쪽 하노이 | 원판 2개 최단 `AA → BA → BC → CC` (3번), n개면 2ⁿ−1 |
| 25쪽 A~J 트리 | BFS `A-B-C-D-G-H-I-E-F-J` (10) · DFS `A-B-D-E-F-C-G-H-I-J` (10) |
| 26쪽 도시 a~e | 최단 `a-c-d-e` · 비용 **12** · OPEN/CLOSED 표 6줄이 학습지와 한 칸씩 일치 |
| 24쪽 8-퍼즐 | 초기 `283104765` · 목표 `123804765` · 최소 4번 이동 |
| 28쪽 8-퍼즐 h(n) | `2 8 3 / 1 6 4 / 7 _ 5` → 맨해튼 1+1+1+2 = **5** |
| 30쪽 A* 격자 | (1,0)→(3,5) 맨해튼 7. 최적 경로 7칸 |
| 32쪽 규칙 추론 | ① 포유류(R1)→육식동물(R5)→**호랑이**(R9) / ② 포유류(R2)→초식동물(R7)→**얼룩말**(R11) |

### ⚠️ 원본이 그림이라 복원하지 못한 것 두 곳

학습지의 해당 부분이 **이미지**여서 값을 읽을 수 없었다. 대신 이렇게 처리했다.

1. **25쪽 아래 「A~Z 도로 그래프」** — 연결 관계를 알 수 없어 넣지 않았다.
   대신 같은 것(깊이 우선은 최단 경로를 보장하지 못한다)을 가르치는
   「지름길이 있는 도로」 그래프를 새로 만들어 넣었다.
   → BFS 3번 방문·경로 1칸 / DFS 8번 방문·경로 7칸으로 대비가 더 뚜렷하다.

2. **30쪽 6×6 격자의 장애물 배치** — 정답에 적힌 f 값 조각
   (`0+7` `1+8` `2+7` `1+6` `2+5` `3+4` `4+3` `4+5` `5+2` `6+1`)과
   **앞뒤가 모두 맞는** 배치를 찾아 기본값으로 넣었다.
   출발 (1,0) · 도착 (3,5) · 벽 `(0,2) (1,1) (3,0) (3,1) (2,4) (4,3)`.
   격자는 눌러서 편집할 수 있으니, 원본 배치를 아시면 그대로 옮겨 쓰면 된다.

3. **11쪽 야구 의사결정트리** — 잎(마지막 칸)의 이름까지는 알 수 없었다.
   정답으로 주어진 길(타율 0.210 · 방어율 0.8 → **선발 투수**)이 그대로 나오도록
   나머지 잎을 「타자 / 중간 계투 / 마무리 투수」로 채웠다.

### 학습지 8쪽 표기에 대한 메모

학습지 8쪽 평가지표 표의 **정밀도·재현율 설명이 서로 바뀌어** 적혀 있다.
(「정밀도: 실제 Positive 중 …」 / 「재현율: Positive로 예측한 것 중 …」)

같은 학습지 **9쪽 [활동 1]의 연결 정답**과 **[활동 3]의 계산 정답**
(정밀도 15/25, 재현율 15/20)은 표준 정의와 일치하므로,
앱은 **9쪽 쪽을 따랐다**. 채점되는 값이 그쪽이기 때문이다.
앱 화면에는 헷갈리지 않도록 「분모를 보세요」 안내를 함께 넣어 두었다.
종이 학습지를 고치실 때 함께 손보시면 좋겠다.

### 저작권
- 원본 PDF 와 그 안의 교과서 발췌 그림(금성·비상·천재·YBM 등)은
  **저장소에 올리지 않는다.** `.gitignore` 에 `*.pdf` 를 넣어 두었다.
- 앱의 그림과 표는 모두 코드로 새로 그렸다.
- 라이선스: **Apache License 2.0** (`LICENSE`, `NOTICE`, 소스 헤더 주석)

### 실행
```bash
cd D:\project_AI\22aiBasics_trmoo
npm install
npm start        # 개발 서버 (http://localhost:5174)
npm run build    # dist/index.html 한 파일
npm test         # 학습지 정답 98가지 대조
```
